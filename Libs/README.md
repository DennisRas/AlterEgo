# Libraries

- For development populate `Libs/` locally before loading the addon.
- You can find paths and pins in `.pkgmeta`.
- Check out each external into the matching path.
- If using a local library, add it with a junction, symlink, or submodule.
